# The name of this file shouldn't be changed, or else the Setup Assistant won't detect it
sensors:
  - filtered_cloud_topic: /self_filtered_cloud
    max_range: 5.0
    max_update_rate: 1.0
    padding_offset: 0.1
    padding_scale: 1.0
    point_cloud_topic: /joint_cameras_point_cloud
    point_subsample: 1
    sensor_plugin: occupancy_map_monitor/PointCloudOctomapUpdater